version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Build
    task:
      jobs:
        - name: Build and deploy docker container
          commands:
            - gradle build
            - echo "$DOCKER_PASSWORD" | docker login  --username "$DOCKER_USERNAME" --password-stdin
            - 'docker pull "$DOCKER_USERNAME"/semaphore-demo-java-spring:latest || true'
            - 'docker build --cache-from "$DOCKER_USERNAME"/monda-calc:latest \'
            - '      -t "$DOCKER_USERNAME"/monda-calc:latest .'
            - 'docker push "$DOCKER_USERNAME"/monda-calc:latest'
      secrets:
        - name: dockerhub
